/**
 * Library barrel. Concrete exports are added as subsequent tasks land.
 * This placeholder keeps the TypeScript project compilable while the
 * module skeleton is being filled in.
 */
export const ADAPTER_NAME = "codex-responses-adapter";
